[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && cd apps/api && npx prisma generate && cd ../.. && pnpm --filter @rr/api build"

[deploy]
startCommand = "cd /app/apps/api && npx prisma db push --skip-generate --accept-data-loss && node /app/apps/api/dist/main"
healthcheckPath = "/api/v1/auth/me"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
