[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @rr/api build"

[deploy]
startCommand = "pnpm --filter @rr/api start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
