# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: branding.spec.ts >> Branding Page — /dashboard/admin/branding >> ADMIN can access branding page
- Location: e2e/branding.spec.ts:39:7

# Error details

```
Error: expect(page).not.toHaveURL(expected) failed

Expected pattern: not /\/login/
Received string: "http://localhost:3001/login"
Timeout: 5000ms

Call log:
  - Expect "not toHaveURL" with timeout 5000ms
    9 × unexpected value "http://localhost:3001/login"

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e3]:
    - img
    - generic [ref=e4]:
      - img "Rafinha Running" [ref=e6]
      - paragraph [ref=e7]: Painel do Treinador
    - generic [ref=e8]:
      - heading "Bem-vindo de volta" [level=2] [ref=e9]
      - generic [ref=e10]:
        - generic [ref=e11]:
          - generic [ref=e12]: E-mail
          - textbox "seu@email.com" [ref=e13]
        - generic [ref=e14]:
          - generic [ref=e15]: Senha
          - generic [ref=e16]:
            - textbox "••••••••" [ref=e17]
            - button [ref=e18]:
              - img [ref=e19]
        - button "Entrar" [ref=e22] [cursor=pointer]
    - paragraph [ref=e23]:
      - text: Não tem conta?
      - link "Criar conta de treinador →" [ref=e24] [cursor=pointer]:
        - /url: /register
    - paragraph [ref=e25]: RR — Rafinha Running v1.0.0
  - region "Notifications alt+T"
  - alert [ref=e26]
```

# Test source

```ts
  1   | /**
  2   |  * E2E tests for White-label Branding page.
  3   |  *
  4   |  * Tests:
  5   |  * - Branding page accessible to ADMIN only
  6   |  * - Niche picker renders 6 options
  7   |  * - Color pickers visible
  8   |  * - Upload areas present
  9   |  * - Save button present
  10  |  * - COACH redirected away from branding page
  11  |  */
  12  | 
  13  | import { test, expect } from '@playwright/test';
  14  | 
  15  | async function injectUser(page: any, role: string) {
  16  |   await page.goto('/login');
  17  |   await page.evaluate((r: string) => {
  18  |     localStorage.setItem('rr_access_token', 'mock-token');
  19  |     localStorage.setItem('rr_user', JSON.stringify({
  20  |       id: 'user-1',
  21  |       name: 'Test User',
  22  |       email: 'test@test.com',
  23  |       role: r,
  24  |       branding: { primaryColor: '#DC2626', secondaryColor: '#1F2937', niche: 'running' },
  25  |     }));
  26  |   }, role);
  27  | }
  28  | 
  29  | test.describe('Branding Page — /dashboard/admin/branding', () => {
  30  |   test('COACH is redirected away from branding page', async ({ page }) => {
  31  |     await injectUser(page, 'COACH');
  32  |     await page.goto('/dashboard/admin/branding');
  33  |     await page.waitForLoadState('networkidle').catch(() => {});
  34  | 
  35  |     // Should redirect to /dashboard (role guard)
  36  |     await expect(page).toHaveURL(/\/dashboard$/, { timeout: 5_000 });
  37  |   });
  38  | 
  39  |   test('ADMIN can access branding page', async ({ page }) => {
  40  |     await injectUser(page, 'ADMIN');
  41  |     await page.goto('/dashboard/admin/branding');
  42  |     await page.waitForLoadState('networkidle').catch(() => {});
  43  | 
  44  |     // Should stay on branding page or show branding content
> 45  |     await expect(page).not.toHaveURL(/\/login/);
      |                            ^ Error: expect(page).not.toHaveURL(expected) failed
  46  |   });
  47  | 
  48  |   test('branding page shows niche selector with 6 sport options', async ({ page }) => {
  49  |     await injectUser(page, 'ADMIN');
  50  |     await page.goto('/dashboard/admin/branding');
  51  |     await page.waitForLoadState('networkidle').catch(() => {});
  52  | 
  53  |     // Check for niche options
  54  |     const niches = ['Corrida', 'CrossFit', 'Natação', 'Triátlon', 'Ciclismo', 'Outro'];
  55  |     for (const niche of niches) {
  56  |       await expect(page.getByText(niche)).toBeVisible({ timeout: 5_000 });
  57  |     }
  58  |   });
  59  | 
  60  |   test('branding page shows primary color section', async ({ page }) => {
  61  |     await injectUser(page, 'ADMIN');
  62  |     await page.goto('/dashboard/admin/branding');
  63  |     await page.waitForLoadState('networkidle').catch(() => {});
  64  | 
  65  |     await expect(page.getByText(/cor principal|cor primária|primary/i)).toBeVisible({ timeout: 5_000 });
  66  |   });
  67  | 
  68  |   test('branding page shows secondary color section', async ({ page }) => {
  69  |     await injectUser(page, 'ADMIN');
  70  |     await page.goto('/dashboard/admin/branding');
  71  |     await page.waitForLoadState('networkidle').catch(() => {});
  72  | 
  73  |     await expect(page.getByText(/cor secundária|secondary/i)).toBeVisible({ timeout: 5_000 });
  74  |   });
  75  | 
  76  |   test('branding page shows save button', async ({ page }) => {
  77  |     await injectUser(page, 'ADMIN');
  78  |     await page.goto('/dashboard/admin/branding');
  79  |     await page.waitForLoadState('networkidle').catch(() => {});
  80  | 
  81  |     await expect(page.getByRole('button', { name: /salvar|save/i })).toBeVisible({ timeout: 5_000 });
  82  |   });
  83  | 
  84  |   test('selecting a niche highlights the selected card', async ({ page }) => {
  85  |     await injectUser(page, 'ADMIN');
  86  |     await page.goto('/dashboard/admin/branding');
  87  |     await page.waitForLoadState('networkidle').catch(() => {});
  88  | 
  89  |     // Click CrossFit niche
  90  |     const crossfitBtn = page.getByRole('button').filter({ hasText: 'CrossFit' });
  91  |     if (await crossfitBtn.count() === 0) return;
  92  | 
  93  |     await crossfitBtn.click();
  94  | 
  95  |     // The card should now show as selected (has ring/border class)
  96  |     const crossfitCard = page.locator('[data-niche="crossfit"], button:has-text("CrossFit")').first();
  97  |     await expect(crossfitCard).toBeVisible();
  98  |   });
  99  | 
  100 |   test('primary color preset swatches are clickable', async ({ page }) => {
  101 |     await injectUser(page, 'ADMIN');
  102 |     await page.goto('/dashboard/admin/branding');
  103 |     await page.waitForLoadState('networkidle').catch(() => {});
  104 | 
  105 |     // Color input should exist
  106 |     const colorInput = page.locator('input[type="color"]').first();
  107 |     if (await colorInput.count() > 0) {
  108 |       await expect(colorInput).toBeVisible();
  109 |     }
  110 |   });
  111 | });
  112 | 
```