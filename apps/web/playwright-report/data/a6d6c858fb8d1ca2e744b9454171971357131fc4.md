# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: dashboard.spec.ts >> Dashboard — ADMIN role >> demo mode toggle visible for ADMIN
- Location: e2e/dashboard.spec.ts:77:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: /demo/i })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('button', { name: /demo/i })

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e3]:
    - img
    - generic [ref=e4]:
      - img "Rafinha Running" [ref=e6]
      - paragraph [ref=e7]: Painel do Treinador
    - generic [ref=e8]:
      - heading "Bem-vindo de volta" [level=2] [ref=e9]
      - generic [ref=e10]:
        - generic [ref=e11]:
          - generic [ref=e12]: E-mail
          - textbox "seu@email.com" [ref=e13]
        - generic [ref=e14]:
          - generic [ref=e15]: Senha
          - generic [ref=e16]:
            - textbox "••••••••" [ref=e17]
            - button [ref=e18]:
              - img [ref=e19]
        - button "Entrar" [ref=e22] [cursor=pointer]
    - paragraph [ref=e23]:
      - text: Não tem conta?
      - link "Criar conta de treinador →" [ref=e24] [cursor=pointer]:
        - /url: /register
    - paragraph [ref=e25]: RR — Rafinha Running v1.0.0
  - region "Notifications alt+T"
  - alert [ref=e26]
```

# Test source

```ts
  1   | /**
  2   |  * E2E tests for the Coach Dashboard.
  3   |  *
  4   |  * Tests:
  5   |  * - Dashboard loads and shows sidebar navigation
  6   |  * - Sidebar navItems filtered by role (COACH sees coach items, not admin items)
  7   |  * - Stats cards render
  8   |  * - Demo mode toggle visible for ADMIN
  9   |  * - Error state shows when API fails
  10  |  */
  11  | 
  12  | import { test, expect } from '@playwright/test';
  13  | 
  14  | async function injectCoach(page: any, role = 'COACH') {
  15  |   await page.goto('/login');
  16  |   await page.evaluate((r: string) => {
  17  |     localStorage.setItem('rr_access_token', 'mock-coach-token');
  18  |     localStorage.setItem('rr_user', JSON.stringify({
  19  |       id: 'coach-1',
  20  |       name: 'Rafinha',
  21  |       email: 'rafinha@test.com',
  22  |       role: r,
  23  |       branding: { primaryColor: '#DC2626', secondaryColor: '#1F2937', niche: 'running' },
  24  |     }));
  25  |   }, role);
  26  | }
  27  | 
  28  | test.describe('Dashboard — /dashboard', () => {
  29  |   test.beforeEach(async ({ page }) => {
  30  |     await injectCoach(page, 'COACH');
  31  |     await page.goto('/dashboard');
  32  |     await page.waitForLoadState('networkidle').catch(() => {});
  33  |   });
  34  | 
  35  |   test('loads without crashing', async ({ page }) => {
  36  |     await expect(page).not.toHaveURL(/\/error/);
  37  |   });
  38  | 
  39  |   test('sidebar shows coach navigation items', async ({ page }) => {
  40  |     // COACH should see these items
  41  |     await expect(page.getByRole('link', { name: /atletas/i })).toBeVisible({ timeout: 5_000 });
  42  |     await expect(page.getByRole('link', { name: /planilhas/i })).toBeVisible({ timeout: 5_000 });
  43  |   });
  44  | 
  45  |   test('sidebar does NOT show admin-only items for COACH', async ({ page }) => {
  46  |     // COACH should NOT see admin items
  47  |     const adminLink = page.getByRole('link', { name: /planos & preços/i });
  48  |     await expect(adminLink).not.toBeVisible();
  49  | 
  50  |     const brandingLink = page.getByRole('link', { name: /white-label/i });
  51  |     await expect(brandingLink).not.toBeVisible();
  52  |   });
  53  | 
  54  |   test('shows logout button', async ({ page }) => {
  55  |     const logoutBtn = page.locator('button[title="Sair"], button:has-text("Sair")').first();
  56  |     await expect(logoutBtn).toBeVisible({ timeout: 5_000 });
  57  |   });
  58  | 
  59  |   test('demo mode toggle NOT visible for COACH', async ({ page }) => {
  60  |     const demoBtn = page.getByRole('button', { name: /demo/i });
  61  |     await expect(demoBtn).not.toBeVisible();
  62  |   });
  63  | });
  64  | 
  65  | test.describe('Dashboard — ADMIN role', () => {
  66  |   test.beforeEach(async ({ page }) => {
  67  |     await injectCoach(page, 'ADMIN');
  68  |     await page.goto('/dashboard');
  69  |     await page.waitForLoadState('networkidle').catch(() => {});
  70  |   });
  71  | 
  72  |   test('sidebar shows admin navigation items for ADMIN', async ({ page }) => {
  73  |     await expect(page.getByRole('link', { name: /planos & preços/i })).toBeVisible({ timeout: 5_000 });
  74  |     await expect(page.getByRole('link', { name: /white-label/i })).toBeVisible({ timeout: 5_000 });
  75  |   });
  76  | 
  77  |   test('demo mode toggle visible for ADMIN', async ({ page }) => {
  78  |     const demoBtn = page.getByRole('button', { name: /demo/i });
> 79  |     await expect(demoBtn).toBeVisible({ timeout: 5_000 });
      |                           ^ Error: expect(locator).toBeVisible() failed
  80  |   });
  81  | });
  82  | 
  83  | test.describe('Dashboard — Error State', () => {
  84  |   test('shows error banner when API is unreachable', async ({ page }) => {
  85  |     await injectCoach(page, 'COACH');
  86  |     await page.goto('/dashboard');
  87  | 
  88  |     // The dashboard loads with an error state when the API returns errors
  89  |     // With mock token, API will return 401, triggering refresh, then logout
  90  |     // We just verify the page doesn't show an unhandled JS error
  91  |     await expect(page).not.toHaveURL(/\/error/);
  92  |   });
  93  | });
  94  | 
  95  | test.describe('Unauthenticated Access', () => {
  96  |   test('redirects to /login when accessing /dashboard without token', async ({ page }) => {
  97  |     // Clear any stored auth
  98  |     await page.goto('/login');
  99  |     await page.evaluate(() => {
  100 |       localStorage.clear();
  101 |     });
  102 |     await page.goto('/dashboard');
  103 |     await expect(page).toHaveURL(/\/login/, { timeout: 10_000 });
  104 |   });
  105 | 
  106 |   test('redirects to /athlete-login when accessing /athlete without token', async ({ page }) => {
  107 |     await page.goto('/athlete-login');
  108 |     await page.evaluate(() => {
  109 |       localStorage.clear();
  110 |     });
  111 |     await page.goto('/athlete');
  112 |     await expect(page).toHaveURL(/\/athlete-login/, { timeout: 10_000 });
  113 |   });
  114 | });
  115 | 
```