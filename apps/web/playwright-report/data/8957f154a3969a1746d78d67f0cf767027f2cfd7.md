# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> Login — /login >> shows error for wrong credentials
- Location: e2e/login.spec.ts:22:7

# Error details

```
Test timeout of 20000ms exceeded.
```

```
Error: locator.fill: Test timeout of 20000ms exceeded.
Call log:
  - waiting for getByPlaceholder(/senha|password/i)

```

# Page snapshot

```yaml
- generic [ref=e1]:
  - generic [ref=e3]:
    - img
    - generic [ref=e4]:
      - img "Rafinha Running" [ref=e6]
      - paragraph [ref=e7]: Painel do Treinador
    - generic [ref=e8]:
      - heading "Bem-vindo de volta" [level=2] [ref=e9]
      - generic [ref=e10]:
        - generic [ref=e11]:
          - generic [ref=e12]: E-mail
          - textbox "seu@email.com" [active] [ref=e13]: nobody@invalid.com
        - generic [ref=e14]:
          - generic [ref=e15]: Senha
          - generic [ref=e16]:
            - textbox "••••••••" [ref=e17]
            - button [ref=e18]:
              - img [ref=e19]
        - button "Entrar" [ref=e22] [cursor=pointer]
    - paragraph [ref=e23]:
      - text: Não tem conta?
      - link "Criar conta de treinador →" [ref=e24] [cursor=pointer]:
        - /url: /register
    - paragraph [ref=e25]: RR — Rafinha Running v1.0.0
  - region "Notifications alt+T"
  - alert [ref=e26]
```

# Test source

```ts
  1   | /**
  2   |  * E2E tests for the Login flow.
  3   |  *
  4   |  * Tests:
  5   |  * - Login page renders correctly
  6   |  * - Invalid credentials show error
  7   |  * - Empty fields show validation
  8   |  * - Valid credentials redirect to dashboard (if API available)
  9   |  * - Athlete login page is separate from coach login
  10  |  */
  11  | 
  12  | import { test, expect } from '@playwright/test';
  13  | 
  14  | test.describe('Login — /login', () => {
  15  |   test('renders login form with email and password fields', async ({ page }) => {
  16  |     await page.goto('/login');
  17  |     await expect(page.getByPlaceholder(/e-mail|email/i)).toBeVisible();
  18  |     await expect(page.getByPlaceholder(/senha|password/i)).toBeVisible();
  19  |     await expect(page.getByRole('button', { name: /entrar|login/i })).toBeVisible();
  20  |   });
  21  | 
  22  |   test('shows error for wrong credentials', async ({ page }) => {
  23  |     await page.goto('/login');
  24  |     await page.getByPlaceholder(/e-mail|email/i).fill('nobody@invalid.com');
> 25  |     await page.getByPlaceholder(/senha|password/i).fill('WrongPassword123!');
      |                                                    ^ Error: locator.fill: Test timeout of 20000ms exceeded.
  26  |     await page.getByRole('button', { name: /entrar|login/i }).click();
  27  | 
  28  |     // Should show an error message — wait up to 5s
  29  |     await expect(
  30  |       page.getByText(/credenciais|inválid|incorrect|invalid|erro/i),
  31  |     ).toBeVisible({ timeout: 8_000 });
  32  |   });
  33  | 
  34  |   test('shows error for empty fields', async ({ page }) => {
  35  |     await page.goto('/login');
  36  |     await page.getByRole('button', { name: /entrar|login/i }).click();
  37  | 
  38  |     // Either HTML5 validation or custom error should appear
  39  |     const emailField = page.getByPlaceholder(/e-mail|email/i);
  40  |     const isRequired = await emailField.evaluate((el: HTMLInputElement) => el.validity.valueMissing);
  41  |     expect(isRequired).toBe(true);
  42  |   });
  43  | 
  44  |   test('password field hides text by default', async ({ page }) => {
  45  |     await page.goto('/login');
  46  |     const passwordField = page.getByPlaceholder(/senha|password/i);
  47  |     await expect(passwordField).toHaveAttribute('type', 'password');
  48  |   });
  49  | 
  50  |   test('shows toggle to reveal password', async ({ page }) => {
  51  |     await page.goto('/login');
  52  |     // Look for a show/hide password button
  53  |     const toggleBtn = page.locator('button').filter({ has: page.locator('svg') }).first();
  54  |     // Password is hidden by default
  55  |     const passwordField = page.getByPlaceholder(/senha|password/i);
  56  |     await expect(passwordField).toHaveAttribute('type', 'password');
  57  |   });
  58  | 
  59  |   test('has link to athlete login page', async ({ page }) => {
  60  |     await page.goto('/login');
  61  |     const athleteLink = page.getByRole('link', { name: /atleta|athlete/i });
  62  |     if (await athleteLink.count() > 0) {
  63  |       await expect(athleteLink).toBeVisible();
  64  |     }
  65  |   });
  66  | 
  67  |   test('redirects authenticated user away from login', async ({ page }) => {
  68  |     // Inject auth state
  69  |     await page.goto('/login');
  70  |     await page.evaluate(() => {
  71  |       localStorage.setItem('rr_access_token', 'mock-token');
  72  |       localStorage.setItem('rr_user', JSON.stringify({
  73  |         id: 'u1', name: 'Test Coach', email: 'coach@test.com', role: 'COACH',
  74  |       }));
  75  |     });
  76  |     await page.goto('/login');
  77  |     // The layout should redirect away from login for authenticated users
  78  |     // (behavior depends on layout useEffect — may stay on login if token is invalid)
  79  |     // We just confirm the page doesn't crash
  80  |     await expect(page).not.toHaveURL(/\/error/);
  81  |   });
  82  | });
  83  | 
  84  | test.describe('Athlete Login — /athlete-login', () => {
  85  |   test('renders athlete login form', async ({ page }) => {
  86  |     await page.goto('/athlete-login');
  87  |     await expect(page.getByPlaceholder(/e-mail|email/i)).toBeVisible();
  88  |     await expect(page.getByPlaceholder(/senha|password/i)).toBeVisible();
  89  |     await expect(page.getByRole('button', { name: /entrar|login/i })).toBeVisible();
  90  |   });
  91  | 
  92  |   test('shows error for wrong credentials', async ({ page }) => {
  93  |     await page.goto('/athlete-login');
  94  |     await page.getByPlaceholder(/e-mail|email/i).fill('nobody@invalid.com');
  95  |     await page.getByPlaceholder(/senha|password/i).fill('WrongPassword123!');
  96  |     await page.getByRole('button', { name: /entrar|login/i }).click();
  97  | 
  98  |     await expect(
  99  |       page.getByText(/credenciais|inválid|incorrect|invalid|erro/i),
  100 |     ).toBeVisible({ timeout: 8_000 });
  101 |   });
  102 | });
  103 | 
```